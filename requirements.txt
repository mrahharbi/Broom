# === Core System Libraries ===
psutil==6.0.0          # لجلب معلومات النظام (CPU, RAM, Disk, uptime)
platformdirs==4.3.3     # لتحديد مسارات المجلدات (cache/temp) حسب النظام
pathlib==1.0.1          # لإدارة المسارات والملفات بطريقة حديثة

# === Cleaning and File Operations ===
shutilwhich==2.0.2      # للتعامل مع الملفات والمجلدات بشكل مرن
send2trash==1.8.3       # لحذف الملفات بشكل آمن (ينقلها إلى سلة المهملات)

# === CLI & Logging ===
rich==13.8.0            # لتنسيق الإخراج بالألوان والجداول في سطر الأوامر
click==8.1.7            # لبناء واجهة